// href
console.log($('a').attr('href')); // it will redirect us to the google page
console.log($('a').attr('href', "https://www.yahoo.com")); // we are setting the value of href to the yahoo page

// src
console.log($('img').attr('src'))


// class attribute in the h1 tag
console.log($('h1').attr('class'))  // output : heading